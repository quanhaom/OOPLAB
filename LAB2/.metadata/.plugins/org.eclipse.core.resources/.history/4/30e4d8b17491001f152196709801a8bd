package frame;

import model.Manager;

public class Main {
    public static void main(String[] args) {
        Manager manager = new Manager(null,null);
        RoleSelectionFrame roleSelectionFrame = new RoleSelectionFrame(null,null);
        roleSelectionFrame.setVisible(true);
    }
}
